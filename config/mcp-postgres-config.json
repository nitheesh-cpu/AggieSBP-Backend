{
    "mcpServers": {
        "postgres": {
            "command": "npx",
            "args": [
                "@modelcontextprotocol/server-postgres",
                "postgresql://username:password@VM_IP_ADDRESS:5432/database_name"
            ],
            "env": {
                "POSTGRES_CONNECTION_STRING": "postgresql://username:password@VM_IP_ADDRESS:5432/database_name"
            }
        }
    }
}